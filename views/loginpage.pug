//- PUG TEMPLATE VERSION
doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title(style="color: crimson;") login
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css")
  body(style='background-color: #6f42c1; margin: 0; padding: 0;')
    center
      .container
        .col-md-4
          .card.border-info.shadow-lg(style="background: rgba(101, 67, 33, 0.82); border: none; box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);")
            h1(style="color: #e0ffb3; letter-spacing: 2px; text-shadow: 0 2px 8px #0008;") Y4C SIGNIN
            form#captureform(autocomplete="off" method="POST" action="/login" style="color: #fff;")
              .mb-3
                label.form-label(for="username" style="color:#b3e0ff;")
                  i.fa.fa-user.me-2
                  | Username
                input.form-control.bg-dark.text-light(type="text" placeholder="Enter Username" id="username" name="username" required autofocus aria-required="true" autocomplete="off" style="border-radius:0; background:rgba(30,30,40,0.7); border:1px solid #444;")
              .mb-3.position-relative
                label.form-label(for="password" style="color:#b3e0ff;")
                  i.fa.fa-lock.me-2
                  | Password
                input.form-control.bg-dark.text-light(type="password" placeholder="Enter your password" id="password" name="password" required aria-required="true" autocomplete="new-password" style="border-radius:0; background:rgba(30,30,40,0.7); border:1px solid #444;")
              .mb-3
                button.btn.btn-danger.w-100(type="submit" style="background:rgba(220,20,60,0.85); border:none; box-shadow:0 2px 8px #0005;") Login
            .mt-2.text-danger#loginError(style="display:none;") Invalid credentials. Please try again.
            h5(style="color: #e0b3ff; text-shadow: 0 1px 4px #0007;")
            .mt-3.text-center
              span(style="color:#fff;") Don't have an account?
              a(href="/signup" style="color:#0dcaf0; text-decoration:underline; margin-left:5px;") Register
    script.
      // Clear input fields on page load
      window.addEventListener('DOMContentLoaded', function() {
        document.getElementById('username').value = '';
        document.getElementById('password').value = '';
      });
             